<strong>new</strong>

<%= form_for :accessibles, url: accessibles_path  do |  f| %>
    <p>
      <%= f.label :id %><br>
      <%= f.text_field :id %>
    </p>

    <p>
      <%= f.label :value %><br>
      <%= f.text_area :value %>
    </p>

    <p>
      <%= f.submit %>
    </p>
<% end %>